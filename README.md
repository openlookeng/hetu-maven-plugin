This repository forks from [presto-maven-plugin](https://github.com/prestosql/presto-maven-plugin.git).

hetu-maven-plugin is the Maven build extension providing packing and lifecycle for openLooKeng plugins.

It has additional checkers for Maven to validate the cross dependencies and dependencies on SPI during build.

It additionally generates the service descriptions for the Jars.
